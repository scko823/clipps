language: node_js
node_js:
  - '8'
before_install:
  - node -v
  - npm -v
install:
  - npm install
script:
  - npm test
  - npm run lint
  - npm run build
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: dist/
  github_token: $GITHUB_TOKEN
  on:
    branch: master
